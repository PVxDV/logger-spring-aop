spring:
  datasource:
    username: log
    password: password
    driver-class-name: org.h2.Driver
    name: db
    url: jdbc:h2:mem:db
  jpa:
    properties:
      hibernate:
        ddl-auto: create-drop
    generate-ddl: true
